What is the difference between var, let, and const?
Ans: var, let and const are all variable in JavaScript but var is old version. let has comes in es6. we can reassign let but not redeclare. and const ist use for redeclare or reassign.


What is the spread operator (...)?
Ans: Spread operator open array or object values.

What is the difference between map(), filter(), and forEach()? 
Ans:  map(), filter(), and forEach() all are array methods but it use different aspect. forEach() works for every element, map() works for modify element and return new array and filter() takes element according to condition.


What is an arrow function?
Ans:  Array function is short way to write Js function. 


What are template literals?
Ans: Backtick (``) is called template literals. it is use for writing multiline string. It helps to create dynamic element. 